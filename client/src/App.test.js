import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

  // Reait('renders without crashing', () => {
  // const div = document.createElement('div');
ctDOM.render(<App />, div);
  // ReactDOM.unmountComponentAtNode(div);
});
